# Helm — downloaded dependency tarballs are excluded; run `helm dependency update` after cloning
*.tgz
charts/
# Chart.lock is committed to pin exact dependency versions

# Vault init output — contains root token and unseal key, never commit
vault-init.json

# Secrets & credentials
*.pem
*.key
*.crt
*.p12
*.pfx
secrets.yaml
secrets.yml
*-secret.yaml
*-secret.yml
override-values.yaml

# Terraform (if used alongside)
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Rendered templates (local only)
rendered/
output/
